import './index.css'

function MainOptions() {
  return (
    <div id = "MainOptions">      
      <button class = "MainOption" href = "">All Photos</button>
      <button class = "MainOption" href = "">Filter Photos</button>
    </div>
  );
}

export default MainOptions;
